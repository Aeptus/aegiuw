# Rust
/target
**/*.rs.bk
*.pdb

# Node / Cloudflare Workers
node_modules/
dist/
.wrangler/
.dev.vars
.dev.vars.*
wrangler.toml.bak

# Quality runner artifacts (content-hash cache + per-run logs).
# Cache is reproducible; logs are pruned after 7 days but never committed.
.aeptus-cache/
logs/

# Local tool caches (Chau7 helper, Claude project state)
.chau7/
.claude/

# Secrets / local config (never commit signing keys or device tokens)
*.pem
*.key
secrets/
.env
.env.*

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/*
!.vscode/extensions.json
target/criterion/
