# Pin a consistent toolchain across contributors and CI so `cargo fmt`/`clippy`
# results are reproducible. Bump deliberately.
[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
targets = ["x86_64-unknown-linux-gnu", "aarch64-apple-darwin"]
